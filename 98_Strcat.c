char* mystrcat(char*, char*);
void main()
{
	char arr[5] = "xyz";
	char brr[5];
	printf("%s",mystrcat(brr,arr));
	//printf("%s",brr); also can write
}
char* mystrcat(char* brr, char* arr)
{
	int i = 0;
    int a = 0;
	while(ptr[i] != 0)
	{
		i++;
	}
	brr[i]='\0';
	return brr;
}